[build]
  command = "echo 'Static files only'"
  publish = "."

[functions]
  directory = "functions"

[[edge_functions]]
  function = "classify"
  path = "/api/classify"

[[edge_functions]]
  function = "submit"
  path = "/api/submit"
